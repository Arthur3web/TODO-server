'use strict';

Object.defineProperty(exports, '__esModule', {
	value: true
});
exports['default'] = isToday;

function isToday(date) {
	var options = arguments.length <= 1 || arguments[1] === undefined ? {} : arguments[1];
	var _options$convert = options.convert;
	var convert = _options$convert === undefined ? false : _options$convert;
	var _options$scope = options.scope;
	var scope = _options$scope === undefined ? new Date() : _options$scope;

	if (typeof convert !== 'boolean') {
		throw new TypeError('Expected convert option to be a Boolean got ' + typeof convert);
	}

	if (convert === true) {
		date = new Date(date);
	}

	if (isNaN(Date.parse(date))) {
		throw new TypeError('Expected first parameter to be a Date got ' + typeof date);
	}

	return date.toDateString() === scope.toDateString();
}

;
module.exports = exports['default'];